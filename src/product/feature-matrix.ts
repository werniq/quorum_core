/**
 * Feature matrix re-export for callers that want the matrix only.
 */
export {
  FEATURE_MATRIX,
  featureMatrixMarkdown,
  type FeatureAvailability,
  type FeatureMatrixRow,
} from "./positioning.js";
