# Quorum environment example
# Copy to .env and set secrets before `docker compose up`.

# Credential KEK: supply via secret mechanism. Back up separately from the database file.
# Placeholder only — replace before any real deploy (min 16 characters).
QUORUM_CREDENTIAL_KEK=replace-with-long-random-secret
# QUORUM_CREDENTIAL_KEK_PREVIOUS=optional-previous-key-during-rotation
# Optional distinct key for outcome identifier HMAC (otherwise derived from KEK).
# QUORUM_IDENTIFIER_HMAC_KEY=replace-with-another-long-secret

# HTML UI login/setup. Default true (require setup token + login).
# For a local open catalog without login, either set false or use QUORUM_DEMO_MODE=true.
QUORUM_UI_AUTH_ENABLED=true
# Insecure local demo: opens UI without login. Requires HOST=127.0.0.1|localhost|::1
# (rejected with 0.0.0.0). Do not enable in docker-compose with HOST=0.0.0.0.
# QUORUM_DEMO_MODE=false
# One-time bootstrap token when UI auth is on (≥24 characters). Not the admin password.
# After /setup, create an admin password ≥12 characters (not a known default like password/changeme).
QUORUM_SETUP_TOKEN=replace-with-one-time-setup-token-min-24-chars

PUBLIC_BASE_URL=http://127.0.0.1:3000
# Optional host publish port when 3000 is already in use:
# QUORUM_HOST_PORT=3000

# Metrics disabled by default. When enabled, set a bearer token (or use loopback only).
# METRICS_ENABLED=false
# METRICS_AUTH_TOKEN=

# Dedicated worker intervals
# OUTBOX_INTERVAL_MS=15000
# N8N_POLL_SCHEDULER_INTERVAL_MS=15000
